## Summary

## Validation

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Storage and backup compatibility were reviewed
- [ ] No credentials, real warehouse data, generated builds, or signing files are included

## Remaining browser or device validation
